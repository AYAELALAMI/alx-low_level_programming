#include <stdio.h>

/**
 *  main -  the entery point of the code
 * declaration: print the name of the source file
 *
 * Return: 0 (success).
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
